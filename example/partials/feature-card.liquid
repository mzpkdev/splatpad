<article class="rounded-xl border border-zinc-800 bg-zinc-950 p-6">
  <h2 class="text-xl font-semibold">{{ feature.title }}</h2>
  <p class="mt-3 leading-7 text-zinc-400">{{ feature.description }}</p>
</article>
