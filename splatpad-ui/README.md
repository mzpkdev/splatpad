# Splatpad UI

The Splatpad product interface, built as a Splatpad site.

```sh
node ../bin/splatpad serve splatpad-ui
node ../bin/splatpad build splatpad-ui
```

Pages live in `pages/`, shared layouts in `layouts/`, and site-wide render data
in `data/site.json`.
